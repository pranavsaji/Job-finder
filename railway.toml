[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && /opt/venv/bin/playwright install chromium --with-deps || true"

[deploy]
startCommand = "/opt/venv/bin/uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
